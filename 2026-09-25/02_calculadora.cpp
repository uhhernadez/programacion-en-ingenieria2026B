#include <iostream>
using namespace std;

int main () {
    char operacion;
    cout << "Ingrese operacion (+,-,*,/) :";
    cin >> operacion;
    if (operacion == '+') {
        // Suma
    } else if (operacion == '-') {

    } else if (operacion == '*') {

    } else if (operacion == '/') {

    }
}