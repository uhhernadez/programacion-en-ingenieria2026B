#include <iostream>

int main () {
    float a = 1, b = 0;

    std::cout << "Operacion division por cero : " << a / b << std::endl;
    std::cout << "Operacion de division cero entre cero: " << 0.0/0.0 << std::endl; 
    return 0;
}